name: Download All Files

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
  pull_request:
    branches:
      - main
      - master

jobs:
  bundle-and-upload:
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Create full project archive
        run: |
          mkdir -p dist
          zip -r dist/Angle-smp-plugin-${{ github.run_id }}-${{ github.run_attempt }}.zip . \
            -x ".git/*" \
            -x "dist/*"

      - name: Upload archive artifact
        uses: actions/upload-artifact@v4
        with:
          name: Angle-smp-plugin-${{ github.run_id }}-${{ github.run_attempt }}
          path: dist/Angle-smp-plugin-${{ github.run_id }}-${{ github.run_attempt }}.zip
          if-no-files-found: error
          retention-days: 30
